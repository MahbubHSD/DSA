# Alternate file for top_k_frequent_elements.py

from top_k_frequent_elements import top_k_frequent as _reference_top_k_frequent

def top_k_frequent(arr, k):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_top_k_frequent(arr, k)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'top_k_frequent_elements.py')
