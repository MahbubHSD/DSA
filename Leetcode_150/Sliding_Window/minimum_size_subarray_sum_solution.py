# Alternate file for minimum_size_subarray_sum.py

from minimum_size_subarray_sum import minimum_subarray_length as _reference_minimum_subarray_length

def minimum_subarray_length(target, arr):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_minimum_subarray_length(target, arr)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'minimum_size_subarray_sum.py')
