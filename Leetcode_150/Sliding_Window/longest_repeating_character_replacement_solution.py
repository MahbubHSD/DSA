# Alternate file for longest_repeating_character_replacement.py

from longest_repeating_character_replacement import character_replacement as _reference_character_replacement

def character_replacement(text, k):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_character_replacement(text, k)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'longest_repeating_character_replacement.py')
