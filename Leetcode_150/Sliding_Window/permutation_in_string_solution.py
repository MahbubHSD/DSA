# Alternate file for permutation_in_string.py

from permutation_in_string import contains_permutation as _reference_contains_permutation

def contains_permutation(first, second):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_contains_permutation(first, second)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'permutation_in_string.py')
