# Alternate file for longest_subarray_after_deleting_one.py

from longest_subarray_after_deleting_one import longest_subarray as _reference_longest_subarray

def longest_subarray(arr):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_longest_subarray(arr)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'longest_subarray_after_deleting_one.py')
