# Alternate file for sliding_window_maximum.py

from sliding_window_maximum import max_sliding_window as _reference_max_sliding_window

def max_sliding_window(arr, k):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_max_sliding_window(arr, k)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'sliding_window_maximum.py')
