# Alternate file for minimum_window_substring.py

from minimum_window_substring import minimum_window as _reference_minimum_window

def minimum_window(text, target):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_minimum_window(text, target)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'minimum_window_substring.py')
