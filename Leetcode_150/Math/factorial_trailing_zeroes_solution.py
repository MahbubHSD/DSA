# Alternate file for factorial_trailing_zeroes.py

from factorial_trailing_zeroes import trailing_zeroes as _reference_trailing_zeroes

def trailing_zeroes(n):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_trailing_zeroes(n)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'factorial_trailing_zeroes.py')
