# Alternate file for sort_colors.py

from sort_colors import sort_colors as _reference_sort_colors

def sort_colors(arr):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_sort_colors(arr)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'sort_colors.py')
