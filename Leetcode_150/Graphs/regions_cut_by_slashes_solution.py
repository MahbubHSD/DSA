# Alternate file for regions_cut_by_slashes.py

from regions_cut_by_slashes import regions_by_slashes as _reference_regions_by_slashes

def regions_by_slashes(grid):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_regions_by_slashes(grid)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'regions_cut_by_slashes.py')
