# Alternate file for find_eventual_safe_states.py

from find_eventual_safe_states import eventual_safe_states as _reference_eventual_safe_states

def eventual_safe_states(graph):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_eventual_safe_states(graph)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'find_eventual_safe_states.py')
