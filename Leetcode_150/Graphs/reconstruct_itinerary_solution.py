# Alternate file for reconstruct_itinerary.py

from reconstruct_itinerary import reconstruct_itinerary as _reference_reconstruct_itinerary

def reconstruct_itinerary(tickets):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_reconstruct_itinerary(tickets)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'reconstruct_itinerary.py')
