# Alternate file for critical_connections_in_a_network.py

from critical_connections_in_a_network import critical_connections as _reference_critical_connections

def critical_connections(n, connections):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_critical_connections(n, connections)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'critical_connections_in_a_network.py')
