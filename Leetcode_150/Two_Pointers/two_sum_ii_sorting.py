# Alternate file for two_sum_ii.py

from two_sum_ii import two_sum_sorted as _reference_two_sum_sorted

def two_sum_sorted(numbers, target):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_two_sum_sorted(numbers, target)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'two_sum_ii.py')
