# Alternate file for backspace_string_compare.py

from backspace_string_compare import backspace_compare as _reference_backspace_compare

def backspace_compare(first, second):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_backspace_compare(first, second)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'backspace_string_compare.py')
