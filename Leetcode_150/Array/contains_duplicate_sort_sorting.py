# Alternate file for contains_duplicate_sort.py

from contains_duplicate_sort import contains_duplicate_sort as _reference_contains_duplicate_sort

def contains_duplicate_sort(nums):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_contains_duplicate_sort(nums)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'contains_duplicate_sort.py')
