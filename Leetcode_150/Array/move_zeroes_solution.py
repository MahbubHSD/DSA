# Alternate file for move_zeroes.py

from move_zeroes import move_zeroes as _reference_move_zeroes

def move_zeroes(arr):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_move_zeroes(arr)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'move_zeroes.py')
