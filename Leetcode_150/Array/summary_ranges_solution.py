# Alternate file for summary_ranges.py

from summary_ranges import summary_ranges as _reference_summary_ranges

def summary_ranges(arr):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_summary_ranges(arr)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'summary_ranges.py')
