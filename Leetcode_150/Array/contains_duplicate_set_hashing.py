# Alternate file for contains_duplicate_set.py

from contains_duplicate_set import contains_duplicate_set as _reference_contains_duplicate_set

def contains_duplicate_set(nums):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_contains_duplicate_set(nums)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'contains_duplicate_set.py')
