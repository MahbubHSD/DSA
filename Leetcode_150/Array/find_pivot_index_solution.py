# Alternate file for find_pivot_index.py

from find_pivot_index import pivot_index as _reference_pivot_index

def pivot_index(arr):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_pivot_index(arr)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'find_pivot_index.py')
