# Alternate file for rotate_array.py

from rotate_array import rotate_array as _reference_rotate_array

def rotate_array(arr, k):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_rotate_array(arr, k)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'rotate_array.py')
