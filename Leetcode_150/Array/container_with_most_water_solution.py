# Alternate file for container_with_most_water.py

from container_with_most_water import max_area as _reference_max_area

def max_area(heights):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_max_area(heights)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'container_with_most_water.py')
