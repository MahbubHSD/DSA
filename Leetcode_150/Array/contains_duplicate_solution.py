# Alternate file for contains_duplicate.py

from contains_duplicate import contains_duplicate as _reference_contains_duplicate

def contains_duplicate(arr):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_contains_duplicate(arr)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'contains_duplicate.py')
