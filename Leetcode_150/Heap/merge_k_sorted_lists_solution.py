# Alternate file for merge_k_sorted_lists.py

from merge_k_sorted_lists import merge_k_sorted_lists as _reference_merge_k_sorted_lists

def merge_k_sorted_lists(lists):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_merge_k_sorted_lists(lists)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'merge_k_sorted_lists.py')
