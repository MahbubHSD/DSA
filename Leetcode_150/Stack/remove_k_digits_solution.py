# Alternate file for remove_k_digits.py

from remove_k_digits import remove_k_digits as _reference_remove_k_digits

def remove_k_digits(number, k):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_remove_k_digits(number, k)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'remove_k_digits.py')
