# Alternate file for partition_equal_subset_sum.py

from partition_equal_subset_sum import can_partition as _reference_can_partition

def can_partition(arr):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_can_partition(arr)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'partition_equal_subset_sum.py')
