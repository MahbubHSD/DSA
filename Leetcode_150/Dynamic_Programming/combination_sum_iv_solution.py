# Alternate file for combination_sum_iv.py

from combination_sum_iv import combination_sum_count as _reference_combination_sum_count

def combination_sum_count(numbers, target):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_combination_sum_count(numbers, target)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'combination_sum_iv.py')
