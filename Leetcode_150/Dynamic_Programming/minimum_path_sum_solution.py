# Alternate file for minimum_path_sum.py

from minimum_path_sum import minimum_path_sum as _reference_minimum_path_sum

def minimum_path_sum(grid):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_minimum_path_sum(grid)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'minimum_path_sum.py')
