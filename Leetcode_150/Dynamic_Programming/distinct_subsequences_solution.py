# Alternate file for distinct_subsequences.py

from distinct_subsequences import distinct_subsequences as _reference_distinct_subsequences

def distinct_subsequences(source, target):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_distinct_subsequences(source, target)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'distinct_subsequences.py')
