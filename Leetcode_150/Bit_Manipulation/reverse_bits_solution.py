# Alternate file for reverse_bits.py

from reverse_bits import reverse_bits as _reference_reverse_bits

def reverse_bits(number):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_reverse_bits(number)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'reverse_bits.py')
