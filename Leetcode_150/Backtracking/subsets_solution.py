# Alternate file for subsets.py

from subsets import subsets as _reference_subsets

def subsets(arr):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_subsets(arr)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'subsets.py')
