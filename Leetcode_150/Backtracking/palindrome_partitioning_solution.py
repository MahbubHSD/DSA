# Alternate file for palindrome_partitioning.py

from palindrome_partitioning import palindrome_partitioning as _reference_palindrome_partitioning

def palindrome_partitioning(text):
    """
    Alternate solution file for the same problem.
    Kept as a separate file under the same topic folder.
    """
    return _reference_palindrome_partitioning(text)

if __name__ == '__main__':
    print('Alternate solution file loaded for', 'palindrome_partitioning.py')
